% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zones.R
\name{get_zones}
\alias{get_zones}
\title{Get Spanish Administrative Zones}
\usage{
get_zones(
  level = "dist",
  year = 2023,
  zones_filter = NULL,
  region_filter = NULL,
  city_filter = NULL
)
}
\arguments{
\item{level}{Character. Spatial aggregation level:
\itemize{
\item "dist" - Districts (default, recommended)
\item "muni" - Municipalities
}}

\item{year}{Numeric. Year for the zone geometries (default: 2023)}

\item{zones_filter}{Character vector. Optional filter for specific zone IDs}

\item{region_filter}{Character. Optional filter by region/province name or code}

\item{city_filter}{Character vector. Optional filter by city names (e.g., "Madrid", "Barcelona")}
}
\value{
SF object containing zone geometries with:
\itemize{
\item id - Zone identifier
\item geometry - Spatial geometry (polygons)
}
}
\description{
Downloads Spanish administrative zone geometries with filtering options.
This is the core function for accessing spatial transport zone data.
}
\examples{
\dontrun{
# Get all districts
zones <- get_zones(level = "dist")

# Get Madrid zones
madrid_zones <- get_zones(level = "dist", city_filter = "Madrid")

# Get specific zones
zones <- get_zones(level = "dist", zones_filter = c("28001", "28002"))
}
}
