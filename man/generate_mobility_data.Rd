% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobility.R, R/transport_analysis.R
\name{generate_mobility_data}
\alias{generate_mobility_data}
\title{Generate Synthetic Mobility Data}
\usage{
generate_mobility_data(zones, days = 7, seed = 123, base_demand = 100)

generate_mobility_data(zones, days = 7, seed = 123, base_demand = 100)
}
\arguments{
\item{zones}{SF dataframe of zones to generate mobility between}

\item{days}{Number of days to generate data for (default: 7)}

\item{seed}{Random seed for reproducibility (default: 123)}

\item{base_demand}{Base number of trips per OD pair per day (default: 100)}
}
\value{
Data frame with origin, destination, date, hour, and trip counts

Data frame with origin, destination, date, hour, and trip counts
}
\description{
Creates realistic synthetic mobility data for transport analysis and demonstration.
Includes temporal patterns, peak hour effects, and weekend variations.

Creates realistic synthetic mobility data for transport analysis and demonstration.
Includes temporal patterns, peak hour effects, and weekend variations.
}
\examples{
\dontrun{
zones <- get_zones()
mobility_data <- generate_mobility_data(zones, days = 3)
head(mobility_data)
}
\dontrun{
zones <- get_zones()
mobility_data <- generate_mobility_data(zones, days = 3)
head(mobility_data)
}
}
