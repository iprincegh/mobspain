% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobility.R, R/transport_analysis.R
\name{analyze_zone_characteristics}
\alias{analyze_zone_characteristics}
\title{Analyze Zone Characteristics with Spatial Analysis}
\usage{
analyze_zone_characteristics(
  zones,
  size_categories = c(Small = 5, Medium = 15, Large = Inf)
)

analyze_zone_characteristics(
  zones,
  size_categories = c(Small = 5, Medium = 15, Large = Inf)
)
}
\arguments{
\item{zones}{SF dataframe with zone geometries (must contain geometry column)}

\item{size_categories}{Named vector of size breakpoints in km2. Default: c("Small" = 5, "Medium" = 15, "Large" = Inf)}
}
\value{
List containing size_distribution (data.frame), spatial_plot (ggplot), and distribution_plot (ggplot)

List containing size_distribution (data.frame), spatial_plot (ggplot), and distribution_plot (ggplot)
}
\description{
This function performs spatial analysis of transport zones, including size distribution
analysis and visualization. It categorizes zones by area and provides both spatial
and statistical summaries.

Core functions for transport demand analysis and visualization
Based on the comprehensive vignette analysis
}
\details{
Analyze Zone Characteristics with Spatial Analysis

This function performs spatial analysis of transport zones, including size distribution
analysis and visualization. It categorizes zones by area and provides both spatial
and statistical summaries.
}
\examples{
\dontrun{
zones <- get_zones()
analysis <- analyze_zone_characteristics(zones)
print(analysis$size_distribution)
print(analysis$spatial_plot)
}
\dontrun{
zones <- get_zones()
analysis <- analyze_zone_characteristics(zones)
print(analysis$size_distribution)
print(analysis$spatial_plot)
}
}
